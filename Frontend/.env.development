# Backend local (perfil https de launchSettings.json).
# El API tiene UseHttpsRedirection(), así que apuntar al puerto http (5235)
# provoca un 307 hacia https. Usa el puerto https y confía el certificado de
# desarrollo con:  dotnet dev-certs https --trust
VITE_API_URL=https://localhost:7008
